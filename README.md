# compio

A Python binding for the compio async runtime.

## Development

### Setup

Install dependencies including `test` extras:

```bash
uv sync --extra test
```

### Testing

Run the test suite:

```bash
uv run -m unittest -v
```

